# Decision Ledger — GUI Settings Panel & Kiln Settings Profiles

Spec: `docs/superpowers/specs/2026-07-07-settings-panel-design.md`

IDs are append-only and immutable. To reverse a decision, add a new row
naming the one it supersedes. Visual rows (mockup-backed, with breakpoints)
will be appended during implementation planning when the panel's visual form
is designed; the rows below are the architectural decisions from
brainstorming.

| ID | Decision | Mockup ref | Breakpoints | Spec § | Plan task(s) | Assertion | Visual gate |
|----|----------|------------|-------------|--------|--------------|-----------|-------------|
| D101 | Settings are edited as a JSON overlay applied via `setattr` onto the `config` module, explicitly NOT by editing/rewriting `config.py` and NOT by refactoring call sites to a `Settings` class. | N/A | — | §1 | Tasks 2, 6 | `test_global_overlay_applies`, `test_kiln_overlay_applies_over_defaults` (both setattr overlay values onto config; the negative half — `config.py`/`lib/oven.py` are never rewritten — is enforced by `git diff main -- config.py` and `git diff main -- lib/oven.py` both being empty) | N/A |
| D102 | A single Python schema (`lib/settings_schema.py`) drives both server-side validation and frontend form rendering; the frontend hardcodes NO setting names, explicitly NOT hand-built per-setting form markup. | N/A | — | §2 | Tasks 1, 10 | `test_every_schema_key_exists_in_config`, `test_entry_shapes`; browser gate D112 asserts rendered `.setting-row` count == schema-key count (the no-hardcoded-markup negative half) | N/A |
| D103 | Per-kiln bundles are called "kilns" / "kiln settings profiles", explicitly NOT "profiles" (reserved for firing profiles). | N/A | — | Naming | Tasks 4, 9–11 | grep gate: `grep -n '"profile"' public/assets/js/settings.js` returns nothing (the rejected term never appears in the settings UI); UI copy says "kiln settings profile" | N/A |
| D104 | Hardware settings (GPIO/SPI pins, thermocouple type) stay SSH-only in `config.py`, explicitly NOT exposed in the GUI or kiln overlays (wiring is fixed; only the kiln changes). | N/A | — | §3 | Task 1 | `test_hardware_settings_are_not_exposed` (no GPIO/SPI/TC-type key appears in the schema, so the GUI cannot render or persist one). Plus path-traversal guards on the kiln-name boundary: `test_delete_rejects_traversal_names`, `test_rename_rejects_traversal_old_name`, `test_activate_rejects_traversal_name`, `test_duplicate_from_rejects_traversal_name`. | N/A |
| D105 | Scope split: PID, wattage, emergency temp, TC offset, throttle, rate-control tuning are kiln-scope; cost, units, MQTT, graph, cooling, stall/runaway detection are global-scope — explicitly NOT one flat namespace. | N/A | — | §3 | Task 1 | `test_entry_shapes` (every schema entry declares a `scope` of `kiln` or `global` — not a single flat namespace), mirrored against spec §3 scope tables | N/A |
| D106 | Overlays are sparse (only overridden keys stored); unset keys track `config.py` defaults — explicitly NOT full snapshots of all values. | N/A | — | §1, §4 | Tasks 2, 3 | `test_kiln_overlay_applies_over_defaults` (keys absent from the overlay keep their `config.py` default rather than a stored snapshot), `test_reset_removes_override_and_restores_default` (clearing a key removes it from the overlay, not zeroes it) | N/A |
| D107 | Each setting has a declared apply mode (live / next-firing / restart) surfaced in the UI, explicitly NOT a blanket "restart to apply" and NOT silent partial application. | N/A | — | §2, §5 | Tasks 3, 5 | `test_restart_key_persisted_but_not_applied` (a restart-mode key is saved but NOT setattr'd live — no silent application), `test_pending_reports_unapplied_restart_keys` (unapplied restart keys are surfaced, not applied blindly) | N/A |
| D108 | Safety-critical settings and kiln switching are IDLE-only; `ignore_*` TC-error flags are the sole exception, editable mid-firing behind a typed confirmation — explicitly NOT blanket editability and NOT a blanket firing lockout of the whole panel. | N/A | — | §6 | Tasks 3, 4 | `test_safety_key_blocked_while_firing` (non-ignore safety key rejected mid-firing — not blanket editability), `test_ignore_flag_editable_while_firing_with_confirm` + `test_ignore_flag_blocked_while_firing_without_confirm` (the sole exception fires only behind the typed confirm — not a blanket lockout), `test_activate_blocked_while_firing` (kiln switching is IDLE-only) | N/A |
| D109 | Settings writes are all-or-nothing per request with per-key validation errors, explicitly NOT best-effort partial persistence. | N/A | — | §5 | Task 3 | `test_all_or_nothing_on_any_invalid_value` (one invalid key rejects the whole request and persists nothing — no partial write) | N/A |
| D110 | GUI restart button relies on systemd `Restart=always` (unit file fix committed under `deploy/`), explicitly NOT self-exec/respawn logic in Python. | N/A | — | §7 | Tasks 7, 8 | `deploy/kiln-controller.service` ships `Restart=always` + `RestartSec=5`; `/api/settings/restart` handler exits the process for systemd to respawn (comment at kiln-controller.py:770), NOT `os.exec`/respawn in Python; handler returns 409 while the oven is not IDLE | N/A |
| D111 | Settings are reached via a gear icon in the Data Insights widget title bar linking to a standalone settings.html page, explicitly NOT a modal inside index.html. | N/A (textual decision) | 320, 375, 1024 | §8 | Task 9 | — (browser gate) | PASS (screenshots/index-gear-320.png; gear visible + no overlap at 320/375/1024; click → navigates to settings.html) |
| D112 | The settings form is rendered entirely from the server schema — every schema key appears on the page and the JS hardcodes no setting names, explicitly NOT hand-built per-setting markup. | N/A (textual decision) | 1024 | §2, §8 | Tasks 10 | — (browser gate: rendered-row count equals schema-key count) | PASS (screenshots/settings-1024.png; 67 .setting-row === 67 schema keys at 1024) |
| D113 | A kiln bar at the top provides select + Activate/New/Duplicate/Rename/Delete, disabled with an explanatory hint while the oven is not IDLE, explicitly NOT kiln management buried in a submenu. | N/A (textual decision) | 320, 375, 1024 | §8 | Tasks 9, 10 | — (browser gate) | PASS (screenshots/settings-320.png, settings-375.png, settings-1024.png; all 5 controls render + no overflow at 320/375/1024; full CRUD round-trip create/activate/rename/create/activate/delete/reactivate-defaults/delete verified) |
| D114 | Safety-flagged inputs are disabled while firing, EXCEPT ignore_* flags which stay editable behind a typed IGNORE confirmation, explicitly NOT a blanket lockout of the whole panel. | N/A (textual decision) | 1024 | §6, §8 | Task 11 | — (browser gate) | PASS (screenshots/settings-firing-locked-1024.png; while RUNNING: emergency/stall/runaway disabled w/ title, all 12 ignore_* stay enabled; dismiss IGNORE → flag stays false, accept "IGNORE" → flag true) |
| D115 | Saves returning restart-required outcomes raise a persistent banner with a Restart button (driven by server-side restart_pending, so it survives reloads), explicitly NOT a transient toast. | N/A (textual decision) | 320, 1024 | §8 | Task 11 | — (browser gate) | PASS (screenshots/settings-restart-banner-320.png; mqtt_port→8884 Save → banner+Restart button, restart_pending true survives reload; no overflow at 320; reset override + Save → banner clears) |
| D116 | Each settings category renders as its own clearly distinct container card (own surface, border, radius, internal padding), explicitly NOT a flat list with h3 dividers inside one widget; inputs and row text carry comfortable token-based padding. | N/A (user-directed refinement) | 320, 1024 | §8 | UI-fix (post-T11) | — (browser gate) | PASS (screenshots/settings-1024.png, settings-320.png; .settings-category cards bg rgb(37,37,37) vs body rgb(18,18,18), 1px border, 8px radius, header bar; input padding 8px 16px; distinct cards not flat h3 list at 320/1024) |
