# Decision Ledger — Firing Profiles UX (duplicate / drag / insert)

Created 2026-07-07 during brainstorming. Interactive mockup reviewed and approved by Galen
(artifact: claude.ai/code/artifact/1a344485-f071-49c7-b712-128108aaef80).
IDs are append-only and immutable. Later stages fill their own column against the same row.

| ID | Decision | Mockup ref | Breakpoints | Spec § | Plan task(s) | Assertion | Visual gate |
|----|----------|------------|-------------|--------|--------------|-----------|-------------|
| D001 | Profiles panel gets a **Duplicate** button between "+ New" and "Edit" that opens edit mode pre-filled with a deep copy of the selected profile, name "\<name\> (copy)", saved only on Save — explicitly NOT an instant-save copy. | [mockups/D001.png](mockups/D001.png) | 320, 1440 | Feature 1 — Duplicate profile | `#btn_dup` in index.html + `enterDuplicateMode()` (picoreflow.js): deep-copies segments into `profile_segments` via `loadProfileForEditing`, sets `#form_profile_name` = "\<name\> (copy)", opens editor; saved only through unchanged `saveProfile()`. | browser: click `#btn_dup` on "ramp hold slow cool" → name field = "ramp hold slow cool (copy)", segments deep-copied (editing the copy left `profiles[]` source object unchanged); Save created a new file, original file untouched (mtime). Negative half: nothing written to storage before Save (no instant-save copy). | PASS — [gate/gate-D001-320.png](gate/gate-D001-320.png), [gate/gate-D001-1440.png](gate/gate-D001-1440.png). Initial gate FAILed at 320 (third button clipped `.profile-actions`); fixed in 047c3a4 (flex-wrap + tighter padding), re-gated PASS. |
| D002 | Segment rows are reordered by a **grip drag handle in a new far-left column**, implemented with pointer events (works for mouse and touch) — explicitly NOT the ▲▼ move-up/move-down buttons (removed) and NOT HTML5 drag-and-drop. | [mockups/D002.png](mockups/D002.png) | 320, 768, 1440 | Feature 2 — Drag-handle reordering | `updateProfileTable_v2()` rewritten to grid `.seg-row`s with a `.seg-grip` first column; `startSegmentDrag()` uses `pointerdown`/`setPointerCapture`/`pointermove`/`pointerup`/`pointercancel` (no HTML5 DnD), drop index from pointer-Y vs live sibling midpoints (recomputed per pointermove since 047c3a4), then splices `profile_segments`; move-up/move-down handlers removed. | browser: drag row 1 grip below row 2 → order [2,1,3,4], row numbers + Est. recompute; battery of 3 drags (row0→slot2, row3→slot0, row1→slot3) each landed exactly per pointer-Y vs midpoints. Negative half: 0 `.move-up` and 0 `.move-down` elements in the DOM. | PASS — [gate/gate-editor-320.png](gate/gate-editor-320.png), [gate/gate-editor-768.png](gate/gate-editor-768.png), [gate/gate-editor-1440.png](gate/gate-editor-1440.png). Gate found 4-digit values truncated at 320; fixed in 047c3a4 (Est. column hidden ≤359px), re-gated PASS (0 truncated inputs). |
| D003 | Segments are added via **always-visible "+" insert zones** above the first row, between every pair of rows, and after the last row — explicitly NOT hover-revealed zones and NOT the single bottom "+ Segment" button (removed). | [mockups/D003.png](mockups/D003.png) | 320, 768, 1440 | Feature 3 — In-between insert | `insertZoneHtml(pos)` emits an always-visible `.insert-zone`/`.insert-btn` before row 0, after every row, and alone when empty; `insertSegment(pos)` splices at that position; the `#add_segment` bottom button and handler removed. | browser: 4-segment profile shows 5 `.insert-zone` elements (data-pos 0–4), all visible with no hover. Negative half: 0 `#add_segment` button in the DOM. | PASS — same editor screenshots as D002 (all zones visible without hover at 320/768/1440). |
| D004 | A segment inserted between two rows is pre-filled by **interpolating its neighbors**: target = rounded midpoint of previous target (or start temp) and next target; rate = 100 with sign matching direction; hold = 0. Terminal insert keeps today's defaults (prev target + 100, rate 100, hold 0) — explicitly NOT a clone of the previous row and NOT a fixed default mid-list. | N/A (behavioral — values, not layout) | N/A | Feature 3 — In-between insert (Inserted values) | `insertSegment(pos)`: for `pos < length`, target = `Math.round((prev+next)/2)`, rate = `target < prev ? -100 : 100`, hold 0; for terminal `pos === length`, target = prev + 100, rate 100, hold 0. | browser: insert pos0 (start 65 / first 2100) = target 1083 rate +100; mid ascending (2100/2200) = 2150 rate +100; mid descending (2200/2100) = 2150 rate −100; terminal (prev 1400) = 1500 rate +100; all hold 0. Negative half: inserted row is not a clone of the previous row's values and not a fixed mid-list default. | N/A |
| D005 | The per-row **duplicate ⎘ button is removed** from segment rows; row actions are delete × only — explicitly NOT keeping ⎘ alongside the drag handle. | [mockups/D005.png](mockups/D005.png) | 320, 768, 1440 | Feature 2 — Drag-handle reordering (Row actions) | `updateProfileTable_v2()` emits only a single `.del-segment` (×) button per row; the `.dup-segment` handler in `bindSegmentEvents()` removed. | browser: each row has exactly one `.del-segment` (×) which works (delete → 4→3 rows). Negative half: 0 `.dup-segment` (⎘) elements in the DOM. | PASS — same editor screenshots as D002 (delete-only actions at all widths). |

Notes:
- D002/D003/D005 share one editor mockup image; each row's committed copy is its own file per the ledger convention.
- Rejected alternatives were considered live in the mockup: hover-reveal insert zones (rejected for D003 — studio tablet has no hover) and instant-save duplicate (rejected for D001).
- Gate pass 2026-07-07 (fresh agent, real browser, cache disabled): decision-independent smoke check at 320 and 375 — no horizontal overflow, 0 overlapping interactive controls, 0 truncated inputs. Drag re-verified at 1440 after the live-midpoint change ([500,-500] -> [-500,500]).
